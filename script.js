const jmeno = prompt("Uvedte Vase jmeno a prijmeni");
const vek = Number(prompt("Uvedte Vas vek"));

document.body.innerHTML = "<p>" + jmeno + ", vek: " + vek + "</p>"


/*
cyklus ukládání na gitHub:
git add .
git commit -m "text"
git push
*/
